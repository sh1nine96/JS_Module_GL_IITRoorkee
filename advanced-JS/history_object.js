console.log( history );
history.back();
history.forward();
history.go(-2);// value = number of page you want to go back & forth

// history.pushState()  //=> adds an entry to the browser's session history stack. 